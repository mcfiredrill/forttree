<%= form_for(@branch, :html => { :multipart => true }) do |f| %>
  <% if @branch.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@branch.errors.count, "error") %> prohibited this branch from being saved:</h2>

      <ul>
      <% @branch.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="postarea">
    <table>
      <tr>
      <td class="fieldlabel">Name</td>
      <td class="field">
        <input type="text" name="leaf[name]" cols="80" rows="20"><%= @leaf.name%></textarea>
      </td>
      </tr>
      <tr>
      <td class="fieldlabel">Post</td>
      <td class="field">
        <textarea name="leaf[content]" cols="48" rows="4"><%= @leaf.content %></textarea>
      </td>
      </tr>
      <tr>
      <td class="fieldlabel">Photo</td>
      <td class="field">
        <input type="file" name="leaf[photo]"><div class="hide"><%= @leaf.photo%></div></textarea>
      </td>
      </tr>
  </table>
  <div class="actions">
    <%= f.submit %>
  </div>
  </div>
<% end %>

%h1 New leaf
%p
  %b Last post in this thread:
  = @branch.last_post_at

%div.replyheader Posting mode: Reply

= render :partial => "layouts/form", :locals => { :newpost => @leaf, :leaf => @leaf, :branch => @branch }
= render @branch

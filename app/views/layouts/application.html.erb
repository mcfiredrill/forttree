<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <!-- Always force latest IE rendering engine (even in intranet) & Chrome Frame -->
    <meta content="IE=edge,chrome=1" http-equiv="X-UA-Compatible">
    <title><%= BOARDTITLE %></title>
    <%= stylesheet_link_tag "application" %>
    <%= stylesheet_link_tag "mobile" if mobile_device? %>
    <% theme_basenames.each do |t| %>
      <%= stylesheet_link_tag "themes/" + t, :title => t %>
    <% end %>
    <%= javascript_include_tag "application" %>
    <script src="http://cdn.bitmindo.com/dogecoin.min.js"></script>
    <%= csrf_meta_tag %>
  </head>
  <body>
    <header>
      <div id="themes">
        <% theme_basenames.each do |t| %>
          <%= link_to t, "#", class: 'set-theme', data: { theme: "#{t}" } %>
        <% end %>
      </div>
      <h1><%= link_to BOARDTITLE, "/" %></h1>
      <% board_listing.each do |board| %>
        <% if current_board(board) %>
          <%= "/#{board.name}/" %>
        <% else %>
          <%= link_to "/#{board.name}/", board %>
        <% end %>
      <% end %>
      <% flash.each do |key, value| %>
        <div class="flash_#{key}">
          <h2><%=value%></h2>
        </div>
      <% end %>
    </header>
    <a href="https://libreplanet.org/2015"
      ><img src="https://libreplanet.org/2015/assets/img/site_logo_alternate.png"
      alt="Come to LibrePlanet 2015!"/></a>
    <script>
      CoinWidgetCom.go({
        /* make sure you update the wallet_address or you will not get your donations */
        wallet_address: "<%=ENV['DOGE_ADDRESS']%>"
        , currency: "dogecoin"
        , counter: "amount"
        , alignment: "bl"
        , qrcode: true
        , auto_show: false
        , lbl_button: "Donate"
        , lbl_address: "Tip Dogecoin to this Address:"
        , lbl_amount: "DOGE"
      });
    </script>
    <%= yield %>
  <body>
</html>
